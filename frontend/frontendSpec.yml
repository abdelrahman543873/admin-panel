version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16.x
    commands:
      - npm install -g @angular/cli
  pre_build:
    commands:
      - cd frontend
      - yarn
  build:
    commands:
      - npx cross-env NG_DEPLOY_AWS_ACCESS_KEY_ID=AKIARSGL23EJ5J3XNEOK NG_DEPLOY_AWS_SECRET_ACCESS_KEY=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 NG_DEPLOY_AWS_BUCKET=${AWS_S3_BUCKET} NG_DEPLOY_AWS_REGION=${AWS_REGION} ng deploy
